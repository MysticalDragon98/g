import Project from "../../../lib/classes/Project.class";
//* Imports

export default async function configureCommand (project: Project, args: string[], options: any) {
    console.log("Welcome to the Whatsapp Business plugin!")
}