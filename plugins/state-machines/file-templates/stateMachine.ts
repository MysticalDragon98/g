import StateMachine from "../../plugins/state-machines/StateMachine";

interface Args {

}

const {{varCap name}}StateMachine = new StateMachine<Args>({

    async start (args: Args) {

    }

});

export default {{varCap name}}StateMachine;