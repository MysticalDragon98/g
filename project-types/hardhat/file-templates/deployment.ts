import { buildModule } from "@nomicfoundation/hardhat-ignition/modules";

const {{varCap name}}IgnitionModule = buildModule("{{varCap name}}", (m) => {
    

    return {

    };
});

export default {{varCap name}}IgnitionModule;