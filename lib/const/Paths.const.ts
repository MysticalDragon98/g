import { resolve } from "path";

const Paths = {
    projectTypes: resolve(__dirname, '../../project-types'),
    plugins: resolve(__dirname, '../../plugins'),
    gadgets: resolve(__dirname, '../../../../.gadgets')
};
export default Paths;
